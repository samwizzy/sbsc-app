import { MenuSlideDirective } from './menu-slide.directive';

describe('MenuSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuSlideDirective();
    expect(directive).toBeTruthy();
  });
});
