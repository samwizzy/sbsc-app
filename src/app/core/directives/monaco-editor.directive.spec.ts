import { MonacoEditorDirective } from './monaco-editor.directive';

describe('MonacoEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new MonacoEditorDirective();
    expect(directive).toBeTruthy();
  });
});
